import category from './category'
import training from './training'
import homepage from './homepage' // 1. Importe o novo schema

// 2. Adicione 'homepage' na lista
export const schemaTypes = [homepage, category, training]